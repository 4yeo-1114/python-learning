# Python 学习计划（6 周加速版）

> 适用人群：已掌握 C++ 基础（变量、控制流、函数、OOP、数据结构）的学习者
> 目标：能用 Python 独立完成一个 **学生登录管理系统**
> 周期：约 6 周（非连续学习，按任务推进）

---

## 学习策略

- **对比学习**：每次学 Python 特性时，主动对比 C++ 的对应写法，效率翻倍
- **以项目驱动**：每阶段都有一个小练习，避免只看不写
- **不连续学习也 OK**：按模块推进，每个模块相对独立，断了也能接上

---

## 第一阶段：Python 基础速通（第 1 周）

> 目标：快速过一遍 Python 语法，重点感受"和 C++ 哪里不一样"

| 天数 | 主题 | 内容要点 | 练习 |
|------|------|----------|------|
| Day 1 | 环境搭建 & 基础语法 | Python 安装、pip、虚拟环境(venv)、`print`、注释、缩进规则 | 跑通 Hello World，用 venv 创建项目 |
| Day 2 | 变量与数据类型 | `int/float/str/bool`、动态类型、类型转换、`type()` | 写一个温度转换器（C↔F） |
| Day 3 | 容器类型 | `list/tuple/dict/set`、切片、推导式 | C++ 的 vector/map/set 对比练习 |
| Day 4 | 控制流 | `if/elif/else`、`for/while`、`range()`、`enumerate()`、`zip()` | 写一个猜数字游戏 |
| Day 5 | 函数 | `def`、参数默认值、`*args/**kwargs`、lambda、作用域 | 写一个带多种参数形式的计算器 |
| Day 6 | 字符串与文件操作 | 字符串方法、f-string、`open()`、`with` 上下文管理器 | 读写 txt 文件，统计词频 |
| Day 7 | 阶段小结 | 复习 + 综合练习 | 写一个命令行通讯录（增删查改，存文件） |

**关键对比点（C++ → Python）：**
- 无需声明类型 / 无指针 / 无手动内存管理
- `for i in range(n)` vs `for (int i = 0; i < n; i++)`
- 列表推导式 `[x*2 for x in lst]` 一行搞定 C++ 好几行的循环
- 缩进即代码块，没有 `{}`

---

## 第二阶段：Python 进阶特性（第 2 周）

> 目标：掌握 Python 独有的高级特性，这些是 C++ 里没有或差异很大的

| 天数 | 主题 | 内容要点 | 练习 |
|------|------|----------|------|
| Day 8 | 面向对象（上） | `class`、`__init__`、`self`、实例/类属性、`@property` | 用 OOP 改写通讯录 |
| Day 9 | 面向对象（下） | 继承、多态、`super()`、魔术方法 (`__str__`/`__repr__`/`__eq__`) | 设计一个简单的类继承体系 |
| Day 10 | 异常处理 | `try/except/else/finally`、`raise`、自定义异常 | 给通讯录加上健壮的输入校验 |
| Day 11 | 模块与包 | `import`、`__name__`、pip 安装第三方库、`requirements.txt` | 拆分离散项目为多文件结构 |
| Day 12 | 迭代器与生成器 | `iter/next`、`yield`、生成器表达式 | 写一个读取大文件的生成器 |
| Day 13 | 装饰器 | 函数是一等公民、闭包、`@decorator` | 写一个计时装饰器、日志装饰器 |
| Day 14 | 阶段小结 | 综合练习 | 用面向对象写一个简易图书管理系统（命令行版） |

**关键对比点：**
- Python 一切都是对象（包括函数、类本身）
- 多继承 + `super()` 的 MRO 顺序
- 没有 `private/protected`（约定用 `_` 前缀）
- 装饰器 ≈ C++ 里 wrapper 模式，但语法简洁得多

---

## 第三阶段：数据库与数据持久化（第 3 周）

> 目标：学会用 Python 操作数据库，为项目的数据层打好基础

| 天数 | 主题 | 内容要点 | 练习 |
|------|------|----------|------|
| Day 15 | SQLite 入门 | sqlite3 模块、建表、CRUD、参数化查询 | 创建学生表，手动插数据 |
| Day 16 | Python 操作 SQLite | `sqlite3.connect()`、`cursor`、`execute/executemany` | Python 脚本增删查改学生记录 |
| Day 17 | JSON & CSV | `json` 模块、`csv` 模块、序列化/反序列化 | 把数据库数据导出为 JSON/CSV |
| Day 18 | 配置管理 | `configparser`、`os` 模块、环境变量 | 实现从配置文件读取数据库路径 |
| Day 19 | 路径与文件系统 | `pathlib`（强烈推荐！）、`os.path` | 批量文件操作脚本 |
| Day 20 | 日期时间 | `datetime`、`timedelta`、`strftime/strptime` | 给通讯录/图书系统加时间字段 |
| Day 21 | 阶段小结 | 综合巩固 | 将之前的图书/通讯录系统接入数据库 |

---

## 第四阶段：项目准备 — 命令行版学生管理系统（第 4 周）

> 目标：先做一个命令行完整版，把业务逻辑跑通

| 天数 | 主题 | 内容要点 | 练习 |
|------|------|----------|------|
| Day 22 | 需求分析 | 功能模块：学生信息 CRUD、用户注册/登录、权限（管理员 vs 学生） | 画功能结构图，设计数据表 |
| Day 23 | 数据库设计 | 用户表、学生表、密码哈希（`hashlib`） | 写出建表 SQL + 初始化脚本 |
| Day 24 | 用户模块 | 注册、登录、密码加密存储 | 实现用户注册登录逻辑 |
| Day 25 | 学生信息模块 | 增删查改学生信息、模糊搜索 | 实现学生 CRUD |
| Day 26 | 权限与菜单 | 不同用户角色看到不同菜单 | 实现命令行交互菜单 |
| Day 27 | 测试与调试 | `print` 调试、`logging` 模块、`unittest` 入门 | 为系统写单元测试 |
| Day 28 | 命令行版完成 | 整合、边界测试、文档 | 完整可运行的命令行版，存 `D:\python study\projects\student_system_cli\` |

---

## 第五阶段：GUI / Web 界面（第 5 周）

> 目标：给系统加上用户界面，二选一

### 方案 A：Tkinter 桌面应用（推荐，零依赖）

| 天数 | 内容 | 
|------|------|
| Day 29 | Tkinter 基础：窗口、Label、Button、Entry、Frame |
| Day 30 | 布局管理：pack/grid/place |
| Day 31 | 事件绑定、弹窗(messagebox)、菜单栏 |
| Day 32 | Treeview 表格显示学生列表 |
| Day 33 | 整合：用 Tkinter 重写学生管理系统界面 |
| Day 34 | 美化：ttk 主题、图标、打包 exe（pyinstaller） |
| Day 35 | 完成 GUI 版 |

### 方案 B：Flask Web 应用（适合想接触 Web 方向）

| 天数 | 内容 |
|------|------|
| Day 29 | Flask 安装、路由、模板渲染(Jinja2) |
| Day 30 | GET/POST、表单处理、session/cookie |
| Day 31 | Flask-SQLAlchemy 数据库 ORM |
| Day 32 | 蓝图(Blueprint)拆分模块 |
| Day 33 | 前端：Bootstrap 快速美化 |
| Day 34 | 部署：Flask 项目结构最佳实践 |
| Day 35 | 完成 Web 版 |

---

## 第六阶段：项目完善与拓展（第 6 周）

> 目标：打磨项目 + 拓展一两个加分功能

| 天数 | 内容 |
|------|------|
| Day 36 | 代码重构，保证可读性 |
| Day 37 | 加上日志系统（`logging`） |
| Day 38 | 扩展功能：成绩管理模块（录入、查询、平均分排名） |
| Day 39 | 扩展功能：选课系统（学生选课、查看课表） |
| Day 40 | 数据可视化：用 `matplotlib` 画成绩分布图 |
| Day 41 | 完善 README + 项目介绍文档 |
| Day 42 | 最终检查、打包、总结 |

---

## 推荐学习资源

| 资源 | 说明 |
|------|------|
| [Python 官方教程](https://docs.python.org/zh-cn/3/tutorial/) | 权威，中英文都有 |
| 《流畅的 Python》(Fluent Python) | 进阶必读，讲"Python 之道" |
| [realpython.com](https://realpython.com/) | 高质量教程，建议搜 C++ vs Python 对比文章 |
| `python -m pydoc <模块名>` | 命令行直接查文档 |

---

## 最终项目：学生登录管理系统

### 核心功能

```
┌─────────────────────────────────────────────┐
│          学生登录管理系统                      │
├──────────────┬──────────────────────────────┤
│  用户模块     │  ● 注册 / 登录 / 修改密码       │
│              │  ● 角色：管理员 + 普通学生       │
├──────────────┼──────────────────────────────┤
│  学生管理     │  ● 添加 / 删除 / 修改学生信息    │
│  (管理员)     │  ● 按学号/姓名搜索             │
│              │  ● 列表展示 + 分页             │
├──────────────┼──────────────────────────────┤
│  个人信息     │  ● 查看本人信息                │
│  (学生)       │  ● 修改部分个人信息             │
├──────────────┼──────────────────────────────┤
│  扩展功能     │  ● 成绩录入与查询              │
│              │  ● 数据导出 Excel/CSV          │
└──────────────┴──────────────────────────────┘
```

### 技术栈

| 层 | 技术 |
|----|------|
| 语言 | Python 3.10+ |
| 数据库 | SQLite（内嵌，零配置） |
| 密码安全 | hashlib / bcrypt |
| 界面 | Tkinter（桌面）或 Flask + HTML（Web） |
| 打包 | pyinstaller |

---

## 进度自检清单

在每个阶段结束时勾选：

- [ ] **第 1 周**：能独立写出命令行通讯录（增删查改 + 文件存储）
- [ ] **第 2 周**：能用 OOP 写出图书管理系统（多文件结构）
- [ ] **第 3 周**：能用 Python 操作数据库，完成 CRUD
- [ ] **第 4 周**：命令行版学生管理系统完整可运行
- [ ] **第 5 周**：Java GUI 或 Flask Web 版可运行
- [ ] **第 6 周**：项目完善，代码整洁，有 README

---

## 给有 C++ 基础者的特别提醒

| C++ 习惯 | Python 做法 |
|----------|------------|
| `for(int i=0;i<n;i++)` | `for i in range(n):` |
| `vector<int>` | `list`（更像动态数组） |
| `map<K,V>` | `dict` |
| `set<K>` | `set` |
| `new/delete` | 不需要！Python 自动 GC |
| `int a[10]` | `[0]*10` 但更推荐 `list` |
| `std::string s = "hi"` | `s = "hi"` |
| `class A : public B {}` | `class A(B):` |
| `const &` 传引用 | 可变对象传引用，不可变对象传值（自动） |
| 头文件 + `.cpp` | 一个 `.py` 文件搞定 |
| `&&` / `\|\|` / `!` | `and` / `or` / `not`（也支持 `&&` 等） |

---

*计划制定日期：2026-07-09*
*预计完成日期：2026-08-20（约 6 周，可弹性调整）*
